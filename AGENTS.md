# BoomDash Agent Guide

## 项目定位

BoomDash 是一个竖屏优先的 H5 Canvas 游戏。玩家在飞船发射前部署炸弹，在飞行过程中按时引爆炸弹，利用爆炸冲量改变飞船的惯性轨迹，让飞船进入终点区域。

## 游戏目标

- 玩家需要让飞船从出生位置飞入当前关卡的一个或多个终点区域；出生位置是飞船的坐标，不是需要绘制或进入的起点区域。
- 炸弹部署位置和引爆时机共同决定飞船的飞行路线。
- 玩家可以失败、重试、重新部署，并从关卡选择界面进入所有当前可用关卡。
- 当前开发阶段不设置关卡解锁限制，所有登记在关卡目录中的关卡都必须可以直接游玩。

## 后续关卡开发战略

- 规划总规模约为 60 个关卡，采用持续增量开发，不预先创建全部关卡或空的占位关卡；每次只加入一小批已经可以完整游玩的关卡。
- 关卡 ID 和目录顺序一旦发布就保持稳定。新增关卡只追加到统一关卡目录，不通过重排或重编号修改已有进度和入口。
- 每批关卡都必须先完成关卡定义、目标可达性检查、成功与失败流程测试，再进入下一批开发。关卡选择页只展示目录中实际登记且可游玩的关卡。
- 难度围绕炸弹部署位置、引爆时机、飞行距离、纵向空间和终点几何逐步增加。优先通过已有物理规则和关卡数据制造差异，不为填充数量随意引入新的物理规则。
- 关卡节奏建议分阶段推进：前期建立单一终点和基本轨迹判断，中期引入边界闭合目标、自然曲线目标和多终点区域，后期组合远距离飞行、精确时机、复杂曲线和多区域选择。
- 每个关卡都应有明确且可验证的设计意图，并保证在当前炸弹数量、引爆规则和飞船物理下存在可行路线；失败应来自玩家决策，而不是目标定义或镜头问题。

### 终点区域设计

- 终点不绑定椭圆或半椭圆。终点应由一条或多条自然曲线与地图边界闭合，或由曲线自身闭合，形成可判定的区域。
- 一个关卡可以配置多个终点区域；飞船进入任意一个有效区域即可判定成功，多个区域之间可以承担不同路线或难度选择。
- 终点几何必须以世界坐标和结构化关卡数据描述。绘制、飞船进入判定和调试显示必须复用同一份几何定义，不能分别维护近似形状。
- 终点区域应有清晰的边界、合理的可达性和足够的视觉辨识度。地图边界参与闭合时，必须明确使用哪一段边界，避免产生意外的无限区域或越界判定。
- 不设立起点区域。关卡只记录飞船出生坐标及必要的初始状态，不绘制起点轮廓，也不执行起点进入或离开判定。

## 整体风格

- 视觉主题：深色、克制的霓虹太空控制台。
- 主色：深黑背景、青色信息和交互、洋红色选中反馈、荧光绿色终点和成功状态、金色加分终点图样、橙色调试提示。
- 字体：`Orbitron` 用于标题和主要状态，`Share Tech Mono` 用于 HUD、按钮和数据。
- 形状：使用直角矩形面板和细边框，不使用大圆角卡片或营销式英雄区块。
- 背景：深色渐变和少量星点，只作为氛围，不得伪装成障碍物或玩法元素。
- 动效：发射闪光、飞船尾迹、爆炸环和粒子反馈应短促、清晰，并且不能修改物理结果。
- 布局：保持 `390 x 844` 逻辑视口和竖屏优先；横屏时居中显示，不拉伸世界坐标；UI 必须避开安全区域。

## 玩法和实现约束

- 出生数据只用于设置飞船初始位置和状态，不建立、不绘制起点区域。
- 终点使用关卡定义中的曲线闭合区域绘制和判定；渲染、碰撞/进入检测和调试信息不得写死为椭圆或半椭圆。
- 多终点关卡使用统一的“进入任意终点区域即成功”规则，不能为每个关卡重复实现独立判定逻辑。
- 第一关至第五关都使用空旷太空，不添加障碍物、墙体、风场、重力、敌人或路线限制。
- 第六关首次引入陨石带。陨石带由世界坐标中的曲线闭合区域定义，绘制、飞船进入判定和调试显示复用同一份几何数据；飞船进入任意陨石区域立即失败。
- 第七关使用长于第一关、短于第二关的地图，中心终点，中轴两片间隔较大的陨石带；直线进入失败，两次定时爆炸可沿左侧走廊通关。
- 第八关终点在右上角，终点下方有陨石带，飞船只能从特定角度区间进入；直线越顶失败，过晚右转会撞上陨石带。
- 多终点关卡可在更难终点上配置 `bonusScore`；进入该终点在时间与炸弹分数之外获得额外加分。加分终点绘制金色 `+` 奖励图样，普通终点不加。绘制、进入判定和加分归属复用同一份几何。
- 第九关使用长于第一关、短于第二关的地图，中段陨石带阻断直线，左上角终点与右上自闭合圆形终点二选一；圆形终点下方另有陨石门限制入射，并配置额外加分。直线撞石失败，两次定时爆炸可走左侧走廊进左上角，或走右侧走廊转入圆形终点获得加分；过猛纠偏会撞上右侧陨石门。
- 第十关使用长于第七关、短于第二关的地图，右偏小顶闭合终点（约第四关大小），右下与左上交错两片陨石带迫使 S 弯。直线撞下带失败，只向左躲会撞上上带，过晚右转同样撞上上带，两次定时爆炸可沿 S 弯进入右侧小终点。
- 物理逻辑使用固定时间步，渲染层不能修改游戏状态。
- UI 操作必须转换成游戏事件交给模拟层处理。
- 第二关通过更高的世界坐标和相机偏移实现纵向浏览，不通过拉伸 Canvas 实现。
- 炸弹、飞船和特效使用世界坐标；相机只影响绘制和指针坐标转换。
- 关卡选择使用统一关卡目录生成，开发阶段不得增加锁定逻辑。
- 成功评级为 1–3 星，由通关分数判定。基础分由时间（越短越好，权重 0.6）和引爆炸弹数（越少越好，权重 0.4）加权得到，满分 10000；若进入带 `bonusScore` 的终点再叠加额外加分。阈值写在关卡数据的 `star3Score` / `star2Score`；任意成功至少 1 星，失败不记星、不记分。星级只用于结果页和关卡选择展示，不作为解锁条件，并由已保存的 `bestScore` 推导。结果页展示本次分数，有加分时另显示 BONUS，成功时星星按顺序一颗一颗点亮。
- 新增关卡应优先只增加关卡数据和必要的目标几何，不复制流程、物理或 UI 逻辑；关卡特例必须有明确的玩法理由和测试覆盖。
- 保持现有的无网格吸附、炸弹可重叠和炸弹一次性引爆行为。
- 新增功能应优先复用现有 TypeScript、Canvas、DOM UI 和 localStorage 结构。

## 目录约定

- `src/app/`：应用流程、阶段和事件。
- `src/level/`：关卡定义和关卡目录。
- `src/simulation/`：固定时间步模拟和物理规则。
- `src/rendering/`：Canvas 绘制和视觉特效。
- `src/input/`：鼠标、触摸、键盘和相机输入。
- `src/ui/`：HUD、按钮、开始界面、结果面板和调试显示。
- `tests/`：Vitest 单元测试。
- `e2e/`：Playwright 浏览器流程测试。
- `docs/`：版本计划、验收标准和开发记录。

## 文档同步规则

- 每次修改必须同步更新 `AGENTS.md`。
- 更新范围包括：当前进度的完成项、进行项、阻塞项、最后更新时间，以及任何被本次改动影响的玩法约束、目录约定或验证说明。

## 当前进度

> 本节需要随着每次功能变更同步更新。更新代码、测试或文档后，必须更新完成项、进行项、阻塞项和最后更新时间。

最后更新时间：2026-08-27

### 已完成

- [x] 第一关炸弹部署、发射、引爆和结果流程。
- [x] 第二关配置，世界高度约为第一关两倍。
- [x] 固定视口与高世界坐标分离。
- [x] 第二关部署阶段上下滚动地图。
- [x] 飞行阶段相机跟随飞船。
- [x] 滚动地图下的炸弹世界坐标转换。
- [x] 开始界面和关卡选择界面。
- [x] 当前所有登记关卡均可直接进入，不设置锁定条件。
- [x] 第一关成功后通过 `NEXT SECTOR` 进入第二关。
- [x] 结果页返回关卡选择入口。
- [x] 暂停界面返回主界面。
- [x] 不设置或绘制起点区域。
- [x] 终点改为曲线与地图边界闭合的区域；绘制、进入判定和调试复用同一份几何定义。
- [x] 多终点使用统一的“进入任意区域即成功”规则。
- [x] 第三关：地图长度与第一关相同，左上和右上各有一个更小的终点区域。
- [x] 第四关：地图长度与第一关相同，顶部偏左、约为第一关 1/3 大小的单一终点区域。
- [x] 第五关：地图略长于第一关，偏上方正中有一个约为第一关 1/4 大小的自闭合圆形终点；直线穿过可成功，侧向掠过会错过并越顶失败。
- [x] 第六关：沿用第一关地图、出生位置和终点，在中段加入可从两侧绕行的自闭合曲线陨石带；直线进入陨石带失败，两次定时爆炸可从左侧绕行通关。
- [x] 陨石带使用结构化世界几何统一驱动碰撞、曲线边界、`*` 符号填充和调试显示。
- [x] 第六关开始前的一页新手教程轮播（陨石带为禁入区域，进入后立即失败），包含循环动图。
- [x] 通关评分改为时间与炸弹数量加权；结果页展示分数，星级由分数阈值判定。
- [x] 结果页成功时星星按顺序一颗一颗点亮。
- [x] 相机、世界高度和第二关边界单元测试。
- [x] TypeScript 检查和生产构建。
- [x] 第六关目录顺序、陨石碰撞、失败流程、可达路线和教程映射单元测试。
- [x] 第七关：地图高度 1200，中心终点，中轴上下两片间隔较大的陨石带；直线失败，两次定时爆炸沿左侧走廊通关。
- [x] 第八关：右上角终点，终点下方陨石带限制入射角；直线越顶失败，过晚右转撞石，适时右转可通关。
- [x] 第七关、第八关目录顺序、陨石碰撞、失败流程和可达路线单元测试。
- [x] 第九关：中段陨石带加右侧陨石门，左上角与右上圆形双终点；直线失败，两次爆炸可走左或右通关，过猛纠偏撞门。
- [x] 多终点加分：更难终点可配置 `bonusScore`，进入后叠加在时间/炸弹分数上；加分终点绘制金色 `+` 图样，结果页显示 BONUS。
- [x] 第九关右侧圆形终点加分，开始前一页新手教程轮播（奖励图样与额外加分），包含循环动图。
- [x] 第十关：长地图、右偏小顶终点、右下与左上交错陨石带；直线和下带失败，只躲不回或过晚右转撞上带，两次爆炸沿 S 弯通关。
- [x] 第九关、第十关目录顺序、陨石碰撞、失败流程和可达路线单元测试。
- [x] Playwright 完整 Chromium 安装完成；以 headed 模式通过全部 7 个浏览器流程，包括第六关教程和关卡入口。
- [x] 第一关开始前的三页新手教程轮播（目标、部署、飞行）。
- [x] 第二关开始前的一页新手教程轮播（鼠标滚轮或上下滑动浏览地图）。
- [x] 第四关开始前的一页新手教程轮播（时间与炸弹加权评分及星级）。
- [x] 飞行 HUD 改为剩余时间倒计时，最后 3 秒红色闪烁警告。
- [x] 移除速度与比较速度状态栏。
- [x] 通关三星评级：按加权分数判定，结果页与关卡选择展示，不作为解锁条件。

### 进行中

- [ ] 安装 Playwright Chromium Headless Shell 并执行默认无头浏览器端到端测试。
- [ ] 在移动端和桌面端实际检查第二关拖动、滚轮和按钮布局。

### 已知阻塞

- Playwright Headless Shell 下载连续出现 `ECONNRESET`，默认无头端到端命令暂不可用；完整 Chromium 已安装，`npm run e2e -- --headed` 可正常执行并已通过。

## 验证命令

```text
npm test
npm run build
npm run e2e
```

提交改动前至少运行 `npm test` 和 `npm run build`。如果端到端测试因浏览器依赖失败，应在当前进度中记录原因，不要把失败标记为功能通过。
