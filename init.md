# 第一关 H5 游戏开发计划与实现指南

## 1. 文档目的

本文档用于指导第一关的实现和验收。第一关是玩法验证版本，目标不是提供完整的关卡内容，而是验证以下核心循环是否成立：

1. 玩家在飞船启动前自由规划炸弹位置。
2. 飞船从屏幕底部向屏幕顶部运动。
3. 玩家在飞行过程中选择时机引爆炸弹。
4. 爆炸产生的冲击波改变飞船的速度和方向。
5. 飞船进入顶部终点区域后完成关卡。

第一关必须保持简单、可观察、可重复。场景是空旷太空，不加入障碍物、风场、引力变化、路线限制或其他会掩盖核心玩法的系统。

## 2. MVP 范围

### 2.1 必须实现

- H5 竖屏优先布局。
- 一个固定的底部起点。
- 一个固定的顶部终点。
- 起点和终点都使用半椭圆弧线区域表现。
- 玩家可以在画面内任意位置部署炸弹。
- 炸弹不需要彼此保持距离。
- 炸弹可以放在起点附近或终点附近。
- 炸弹不使用网格吸附。
- 飞船启动后进入飞行阶段。
- 飞行阶段可以点击任意已部署炸弹进行引爆。
- 每枚炸弹只能引爆一次。
- 爆炸冲击方向为远离炸弹的位置方向。
- 飞船进入顶部终点半椭圆区域后判定成功。
- 飞船离开可操作区域或超时后判定失败。
- 支持暂停、重试和重新部署。

### 2.2 明确不实现

- 不放置任何障碍物。
- 不实现风场、重力场、磁场或其他环境力场。
- 不实现墙体、反弹墙或路线边界。
- 不限制炸弹之间的距离。
- 不限制炸弹与起点、终点之间的距离。
- 不使用网格或自动吸附。
- 不实现移动平台、敌人、收集物和机关。
- 不实现多关卡、多人、联网、排行榜和账号系统。
- 不在第一版加入复杂的燃料、生命值、升级和技能系统。

## 3. 推荐技术方案

由于第一关只有一个静态画面，没有障碍物和镜头滚动，建议使用原生 Canvas 2D，而不是引入完整游戏引擎。

- 构建工具：Vite。
- 开发语言：TypeScript。
- 游戏画面：Canvas 2D。
- 界面按钮：HTML/CSS DOM 元素。
- 单元测试：Vitest。
- 浏览器流程测试：Playwright。
- 数据保存：`localStorage`。
- 音效：第一版可使用 Web Audio API，音效不是玩法验证的阻塞项。

原生 Canvas 的好处是依赖少、坐标转换直接、物理公式容易调节，也便于观察每一次爆炸对飞船轨迹的影响。如果以后增加大量关卡、复杂碰撞或动画，再考虑引入 Phaser 等引擎。

## 4. 画面和坐标约定

### 4.1 画面形式

- 主要适配手机竖屏，推荐逻辑比例为 9:16。
- 游戏区占据主要屏幕，不能使用营销式首页或独立介绍页代替游戏画面。
- 在横屏设备上，将竖屏游戏区居中显示，左右保留空白区域，不拉伸游戏坐标。
- Canvas 使用逻辑坐标绘制，再根据设备尺寸进行统一缩放。
- `devicePixelRatio` 最大取 2，避免高分辨率手机造成过高渲染开销。
- HTML 按钮需要避开刘海和底部安全区域，使用 `env(safe-area-inset-*)`。

### 4.2 逻辑坐标

第一版可以使用 390 x 844 作为参考逻辑分辨率，也可以在关卡配置中使用 0 到 1 的归一化坐标。运行时建议统一换算到明确的逻辑画布：

```ts
const LOGICAL_WIDTH = 390;
const LOGICAL_HEIGHT = 844;
```

所有关卡位置、炸弹位置和物理半径都使用逻辑坐标。窗口变化只改变渲染缩放，不改变游戏状态。

为了让第一关保持简单，物理层和 Canvas 都使用同一套屏幕坐标：原点在左上角，`x` 向右，`y` 向下。因此向上的速度和冲量的 `y` 分量为负值。不要再额外维护一套上下颠倒的物理坐标，避免输入、物理和绘制之间出现方向转换错误。

## 5. 第一关固定规格

第一关只显示一个完整画面，不做纵向滚动，也不做相机跟随。

| 项目 | 初始值 | 说明 |
| --- | ---: | --- |
| 逻辑画布 | 390 x 844 | 竖屏参考尺寸 |
| 起点中心 | `(195, 810)` | 位于画面底部中央 |
| 终点中心 | `(195, 34)` | 位于画面顶部中央 |
| 起点半椭圆半径 | `rx=125, ry=58` | 底部显示为上半弧 |
| 终点半椭圆半径 | `rx=125, ry=58` | 顶部显示为下半弧 |
| 飞船半径 | `14` | 仅用于绘制和边界判断 |
| 默认炸弹数量 | `5` | 放入配置，可在调试时改为无限 |
| 爆炸半径 | `110` | 第一版的初始调参值 |
| 最大冲击速度变化 | `150` | 近距离爆炸时的最大值 |
| 初始向上速度 | `-55` | 启动时的一次性初速度，屏幕坐标中向上为负 |
| 飞行超时时间 | `15 秒` | 防止飞船永久飞行 |

以上数值只是第一轮调参起点，不应写死在物理函数内部。第一关的最终手感应通过实际运行和参考操作回放确定。

## 6. 起点和终点半椭圆区域

### 6.1 起点

起点位于画面底部中央，绘制一个向上凸起的半椭圆弧，也就是视觉上的 `∩` 形弧线。

- 弧线颜色使用冷色，例如青蓝色。
- 弧线使用较粗描边，并带轻微发光效果。
- 弧线内部可以有低透明度填充，表示起始区域。
- 飞船初始位置位于弧线中央附近。
- 起点只负责出生和视觉提示，不需要额外的进入判定。

### 6.2 终点

终点位于画面顶部中央，绘制起点的镜像弧线，也就是视觉上的 `∪` 形弧线。

- 弧线颜色使用绿色或亮青色，与起点有明显区分。
- 弧线内部显示半透明捕获区域。
- 飞船从下方向上进入该区域时判定成功。
- 成功判定不要求飞船减速，不要求停在弧线中央。
- 第一版只要飞船中心进入有效终点区域即可成功。

终点有效区域建议定义为终点椭圆的下半部分，而不是一个矩形：

```ts
dx = (ship.x - goal.cx) / goal.rx;
dy = (ship.y - goal.cy) / goal.ry;

insideGoal = dx * dx + dy * dy <= 1 && dy >= 0;
```

这里的 `dy >= 0` 表示飞船从画面下方进入终点的下半椭圆。实际实现时应同时考虑飞船半径，避免飞船视觉上已经碰到终点但中心点还没有进入判定区。

## 7. 炸弹部署规则

### 7.1 位置规则

炸弹部署必须真正自由，第一版不加入任何位置限制：

- 可以放在画面内任意位置。
- 可以放在起点弧线内或附近。
- 可以放在终点弧线内或附近。
- 可以放在其他炸弹旁边。
- 可以让多枚炸弹完全重叠。
- 不做网格吸附。
- 不自动修改玩家点击的位置。
- 不因为位置看起来“不合理”而拒绝放置。

唯一的技术处理是点击位置必须在 Canvas 坐标范围内。若想把炸弹放到画面外，第一版不支持，不需要额外扩展世界空间。

### 7.2 数量规则

默认最多 5 枚炸弹。数量只是为了避免玩家无限点击导致玩法无法比较，不代表位置限制。

开发调试时增加一个配置项：

```ts
maxBombs: 5,
unlimitedBombs: false,
```

调试模式可以把 `unlimitedBombs` 设置为 `true`，用于单独测试冲击波和轨迹，不改变正式第一关的默认规则。

### 7.3 放置操作

- 点击空白区域：在准确的点击坐标创建炸弹。
- 按住炸弹并拖动：移动炸弹。
- 点击炸弹：选中炸弹并显示编号。
- 点击删除按钮：删除当前选中的炸弹。
- 点击“清空”：删除全部炸弹。
- 炸弹重叠时，按编号列表选择，不能依赖视觉上区分重叠图标。

炸弹的视觉图标可以小于触摸区域，但实际触摸区域至少为 44 x 44 CSS 像素。部署阶段点击炸弹不能触发爆炸，只能选择或移动。

## 8. 飞行和爆炸规则

### 8.1 启动

点击启动按钮后：

1. 锁定全部炸弹位置。
2. 隐藏部署工具栏，只保留飞行控制和暂停按钮。
3. 飞船获得一次向上的初始速度。
4. 游戏状态从 `planning` 切换为 `flying`。
5. 飞行计时开始。

第一版不加入复杂倒计时。可以有很短的启动闪烁或发射动画，但不能让动画改变物理起始时间。

### 8.2 空间物理

为了保持空旷太空的设定，第一版环境力全部为零：

- 重力为零。
- 风场为零。
- 不加入横向流动。
- 不加入阻力，飞船保持惯性运动。
- 飞船不会自动转向。
- 飞船不会自动寻找终点。

飞船在启动时拥有较小的向上速度，之后保持惯性运动。炸弹爆炸时直接修改飞船速度。速度过大时可以设置一个仅用于数值稳定的上限，但该上限不应改变正常玩法。

### 8.3 冲击计算

爆炸冲击方向为从炸弹指向飞船，也就是远离炸弹：

```ts
delta = ship.position - bomb.position;
distance = length(delta);

if (distance > blastRadius) {
  impulse = Vec2.zero();
} else {
  direction = distance > EPSILON
      ? normalize(delta)
      : new Vec2(0, -1);

  falloff = Math.pow(
    clamp(1 - distance / blastRadius, 0, 1),
    2,
  );

  impulse = direction * maxImpulse * falloff;
}

ship.velocity += impulse;
```

当炸弹与飞船完全重叠时，方向无法通过向量计算得到。为了避免产生 `NaN`，规定此时使用屏幕正上方作为默认方向，即 `(0, -1)`。这不是位置限制，重叠炸弹仍然合法。

炸弹即使在爆炸半径外引爆，也应进入已使用状态并播放完整的爆炸视觉反馈，只是不会对飞船产生明显推动。这样玩家能清楚感受到“时机过早或过晚”的结果。

### 8.4 爆炸时机

- 飞行阶段点击炸弹时立即记录爆炸事件。
- 使用 `pointerdown` 而不是等待 `click`，减少移动端延迟。
- 每枚炸弹只能响应第一次有效引爆操作。
- 爆炸动画可以持续 200 到 300 毫秒，但物理冲量只在事件发生的那个模拟 tick 施加一次。
- 不实现延迟引爆、导火索、连锁爆炸和多段冲击波。

## 9. 游戏状态

第一版只需要以下状态：

```ts
type GamePhase =
  | "planning"
  | "flying"
  | "paused"
  | "success"
  | "failed";
```

状态转换：

```text
planning -> flying -> success
planning -> flying -> failed
flying -> paused -> flying
flying -> paused -> failed
planning -> planning      重新部署
failed -> planning        重新部署
failed -> flying           保留炸弹重试
success -> planning       再玩一次
```

暂停时必须冻结物理时间、飞船位置、爆炸动画和计时器。重新部署会清除炸弹和飞船运行状态；保留部署重试只重置飞船和炸弹的已引爆状态。

建议的数据结构：

```ts
type Vec2 = {
  x: number;
  y: number;
};

type Bomb = {
  id: string;
  position: Vec2;
  state: "armed" | "detonated";
  order: number;
};

type Ship = {
  position: Vec2;
  velocity: Vec2;
  radius: number;
};

type RunState = {
  phase: GamePhase;
  elapsed: number;
  ship: Ship;
  bombs: Bomb[];
};
```

## 10. 游戏循环实现方式

使用 `requestAnimationFrame` 驱动画面，使用固定时间步驱动物理。推荐时间步为 `1 / 120` 秒，若实现初期需要简单化，也可以使用 `1 / 60` 秒。

```ts
let accumulator = 0;
let previousTime = performance.now();

function frame(now: number) {
  const frameDelta = Math.min((now - previousTime) / 1000, 0.1);
  previousTime = now;
  accumulator += frameDelta;

  while (accumulator >= FIXED_DT) {
    updateFixed(FIXED_DT);
    accumulator -= FIXED_DT;
  }

  render(accumulator / FIXED_DT);
  requestAnimationFrame(frame);
}
```

`updateFixed` 的职责保持简单：

1. 读取当前 tick 的输入事件。
2. 处理炸弹引爆并修改飞船速度。
3. 更新飞船位置。
4. 更新飞行计时。
5. 检查是否进入终点区域。
6. 检查是否离开画面或超时。
7. 更新爆炸效果的剩余时间。

渲染函数不能修改游戏状态。UI 按钮也不能直接修改飞船坐标，所有操作都转换为游戏事件交给状态层处理。

## 11. 边界处理

第一关没有可见墙体，也没有碰撞障碍。为了避免飞船离开后玩家无法继续操作，只保留生命周期边界：

- 飞船完全离开左侧或右侧屏幕时失败。
- 飞船向下离开底部时失败。
- 飞船进入顶部终点区域时成功。
- 飞船越过顶部且没有进入终点区域时失败。

这些边界不绘制为墙，不对飞船施加反弹力，也不应该被包装成障碍物。它们只是判断游戏是否结束。

## 12. 界面布局

### 12.1 部署阶段

顶部 HUD：

- 第一关标识。
- 当前炸弹数和最大数量。
- 暂停按钮。

底部操作栏：

- 清空按钮。
- 删除当前炸弹按钮。
- 启动飞船按钮。

Canvas 中只显示必要的起点、终点、炸弹和飞船，不显示障碍路线或复杂装饰。

### 12.2 飞行阶段

顶部 HUD：

- 当前飞行时间。
- 飞船从底部到顶部的进度。
- 剩余炸弹数量。
- 暂停按钮。

底部或侧边显示炸弹编号列表。列表中的炸弹按钮必须与画面中的炸弹一一对应，支持在炸弹重叠或飞船遮挡炸弹时准确引爆。

### 12.3 结果阶段

成功界面显示：

- 到达用时。
- 使用炸弹数量。
- 再次部署按钮。
- 再次尝试按钮。

失败界面显示明确原因，例如“飞船离开画面”或“超过时间”。第一版不需要复杂评分动画。

## 13. 视觉反馈

每个操作都要有明显反馈，但视觉效果不能改变物理结果。

- 炸弹部署：短暂缩放或闪光。
- 选中炸弹：显示编号和爆炸半径虚线圈。
- 飞船启动：尾焰和短暂发射光。
- 炸弹引爆：冲击波圆环、闪光和少量粒子。
- 冲击生效：飞船尾迹方向发生变化。
- 冲击未命中：爆炸仍可见，但飞船不改变轨迹。
- 成功：终点弧线变亮，飞船冻结并播放捕获动画。
- 失败：飞船淡出或碎光消散，不使用复杂破坏动画。

背景可以使用深色太空和少量原创星点。第一版不加入星云、障碍带、能量墙或其他可能被误解为玩法元素的装饰。

## 14. 建议的目录结构

```text
src/
  main.ts
  app/
    GameApp.ts
    GamePhase.ts
    GameEvents.ts
  level/
    LevelDefinition.ts
    level1.ts
  simulation/
    Vec2.ts
    ShipSimulator.ts
    ExplosionSystem.ts
    LifecycleBounds.ts
  rendering/
    CanvasRenderer.ts
    EffectsRenderer.ts
  input/
    PointerInput.ts
    KeyboardInput.ts
  ui/
    Hud.ts
    PlanningControls.ts
    FlightControls.ts
    ResultPanel.ts
  storage/
    ProgressStore.ts
tests/
  Vec2.test.ts
  ExplosionSystem.test.ts
  GoalDetection.test.ts
  GamePhase.test.ts
e2e/
  level1.spec.ts
public/
  assets/
```

第一关应该由数据配置描述，而不是把坐标散落在渲染代码中：

```ts
export const level1 = {
  id: "level-1",
  start: {
    cx: 0.5,
    cy: 0.96,
    rx: 0.32,
    ry: 0.07,
    arc: "upper",
  },
  goal: {
    cx: 0.5,
    cy: 0.04,
    rx: 0.32,
    ry: 0.07,
    arc: "lower",
  },
  maxBombs: 5,
  blastRadius: 110,
  maxImpulse: 150,
  launchVelocity: -55,
  timeLimit: 15,
};
```

## 15. 测试方案

### 15.1 单元测试

- 向量长度、归一化和加法计算正确。
- 爆炸方向始终远离炸弹。
- 距离越远，冲量越小。
- 超出爆炸半径时不产生冲量。
- 炸弹只能引爆一次。
- 重叠炸弹不会产生 `NaN`。
- 终点半椭圆区域的内外判定正确。
- 飞船离开左右和底部范围时失败。
- 飞船越过顶部但没有进入终点时失败。
- 进入终点区域后立即成功。
- 暂停时位置和计时不变化。
- 同一输入事件序列在固定时间步下产生相同结果。

### 15.2 交互测试

- 任意空白位置都能准确放置炸弹。
- 炸弹可以重叠放置。
- 炸弹可以放在起点和终点附近。
- 不存在网格吸附或自动纠正位置。
- 炸弹可以拖动到画面内其他位置。
- 飞行开始后炸弹位置不能再移动。
- 点击炸弹会立即引爆。
- 重复点击已引爆炸弹不会重复施加冲量。
- 炸弹重叠时可以通过编号列表选择。
- 失败后可以保留部署重新尝试。
- 清空和重新部署能恢复初始状态。

### 15.3 设备测试

至少测试以下环境：

- Android Chrome 竖屏。
- iOS Safari 竖屏。
- 桌面 Chrome 鼠标操作。
- 390 x 844、375 x 667 和较宽的手机视口。
- 横屏时游戏区没有变形或按钮重叠。

## 16. 调试功能

开发环境增加 `?debug=1` 开关，显示：

- 飞船坐标和速度。
- 当前爆炸半径。
- 当前模拟 tick。
- 当前帧率。
- 终点判定区域。
- 每枚炸弹的状态。
- 最近一次爆炸产生的冲量向量。

调试模式还应支持：

- 一键重置飞船但保留炸弹。
- 一键恢复所有炸弹为未引爆状态。
- 无限炸弹。
- 记录输入事件。
- 重放一组固定输入。

记录输入事件比保存每一帧位置更适合当前玩法，因为它可以帮助定位“同样的部署和时机为什么在不同设备上结果不同”的问题。

## 17. 开发顺序

### 阶段一：项目骨架

- 初始化 Vite 和 TypeScript。
- 建立全屏 Canvas 和 DOM HUD。
- 完成逻辑坐标到屏幕坐标的转换。
- 完成手机安全区和窗口变化处理。

完成标准：在手机尺寸下显示一块不变形的游戏画布。

### 阶段二：静态关卡

- 绘制底部起点半椭圆。
- 绘制顶部终点半椭圆。
- 绘制飞船初始位置。
- 绘制简单太空背景。

完成标准：不加入任何炸弹和物理时，画面结构已经符合第一关要求。

### 阶段三：炸弹部署

- 实现点击放置。
- 实现拖动移动。
- 实现选择、删除和清空。
- 实现炸弹编号。
- 验证重叠炸弹和终点附近炸弹都能正常存在。

完成标准：玩家可以自由摆放 5 枚炸弹，代码没有任何额外位置校验。

### 阶段四：飞船模拟

- 实现启动状态转换。
- 实现初始向上速度。
- 实现固定时间步。
- 实现飞船位置更新。
- 实现飞行计时和边界失败。

完成标准：飞船可以从底部稳定飞向顶部，并在离开画面时结束。

### 阶段五：爆炸玩法

- 实现飞行阶段炸弹点击。
- 实现径向冲量和距离衰减。
- 实现爆炸动画。
- 实现一次性引爆状态。
- 实现爆炸事件记录。

完成标准：炸弹位置和引爆时机都会改变飞船轨迹，且结果可以重复复现。

### 阶段六：终点和结果

- 实现终点半椭圆区域判定。
- 实现成功状态。
- 实现失败原因。
- 实现暂停、重试和重新部署。
- 保存最佳通关时间。

完成标准：玩家可以完整执行“部署、启动、引爆、到达或失败、重试”的闭环。

### 阶段七：移动端打磨

- 调整触摸命中区域。
- 处理炸弹重叠时的选择。
- 添加音效开关和基础音效。
- 限制粒子数量。
- 在真实手机上检查帧率和布局。

完成标准：核心操作不被 HUD 遮挡，触摸输入不会误触或丢失。

## 18. 第一关验收标准

第一关完成的最低条件如下：

- 画面中只有空旷太空、飞船、炸弹、起点弧区和终点弧区。
- 中间没有任何障碍物或环境力场。
- 炸弹位置完全由玩家决定。
- 炸弹之间可以重叠。
- 炸弹可以放在起点和终点附近。
- 不存在网格吸附。
- 飞船启动后能够持续向上飞行。
- 爆炸会根据炸弹相对飞船的位置产生不同方向的冲击。
- 爆炸时机会影响冲击强弱和飞行轨迹。
- 飞船进入顶部半椭圆区域后成功。
- 飞船离开画面或超时后失败。
- 成功和失败都能重新开始。
- 同一组输入在不同帧率下不会出现明显不同的结果。
- 手机竖屏下可以完成完整一局。

第一关不要求玩家必须使用全部炸弹，也不要求必须经过某条预设路线。这个版本的目的就是观察自由部署和实时引爆本身是否有趣。

## 19. 后续扩展原则

只有在第一关的核心循环被验证后，才考虑加入其他内容。后续关卡应尽量只扩展 `LevelDefinition` 和少量规则配置，不修改飞船、炸弹和输入系统的基本接口。

可在后续阶段考虑的内容包括：

- 不同冲击强度的炸弹。
- 移动目标。
- 多段路线。
- 障碍物和安全区域。
- 风场或重力变化。
- 炸弹链式反应。
- 更复杂的评分和关卡评价。

这些内容全部不属于当前第一关 MVP。
